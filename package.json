{
  "name": "squarespace-markdown-worker",
  "version": "1.0.0",
  "description": "Convert Squarespace pages to Markdown",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "test:generate": "node test/generate-markdown.mjs"
  },
  "devDependencies": {
    "miniflare": "^3.0.0",
    "wrangler": "^4.59.2"
  }
}
